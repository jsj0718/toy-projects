spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      resource: /h2-console

  jpa:
    properties:
      hibernate:
        ddl-auto: create-drop
    show-sql: true