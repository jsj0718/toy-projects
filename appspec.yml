version: 0.0 # CodeDeploy 버전 (프로젝트 버전이 아니면 0.0 이외는 오류 발생)
os: linux
files:
  - source: / # CodeDeploy에서 전달한 파일 중 destination으로 보낼 대상 지정 (/는 전체 파일)
    destination: /home/ec2-user/app2/step2/zip/ # source 파일을 받을 경로
    overwrite: yes # 덮어쓰기 여부